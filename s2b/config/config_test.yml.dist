imports:
    - { resource: config_dev.yml }

doctrine.dbal:
  default_connection:       default
  connections:
    default:
      driver:               PDOMySql
      dbname:               symfony2bundles_test
      user:                 ~
      password:             ~
      host:                 localhost
      port:                 ~

zend.logger:
    priority: debug

web.config:
    toolbar: false

kernel.test: ~
