imports:
    - { resource: config_dev.yml }

doctrine.dbal:
  connections:
    default:
      driver:               PDOMySql
      dbname:               symfony2bundles_test
      user:                 root
      password:             ~
      host:                 localhost
      port:                 ~

zend.logger:
    priority: debug

web.config:
    toolbar: false

kernel.test: ~
