imports:
    - { resource: config.yml }

doctrine.dbal:
  connections:
    default:
      driver:               PDOMySql
      dbname:               symfony2bundles
      user:                 ~
      password:             ~
      host:                 localhost
      port:                 ~
