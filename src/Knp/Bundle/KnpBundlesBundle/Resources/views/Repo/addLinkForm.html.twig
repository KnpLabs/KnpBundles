<form method="post" action="{{ path('add_link') }}" id="add-url">
    {% if error is defined %}
    <div class="error">
      {{ errorMessage | trans }}
    </div>
    {% endif %}
    <label for="url">URL:</label>
    <input type="text" id="url" name="url" value="{% if url is defined %}{{ url }}{% else %}http://{% endif %}" />
    <input type="hidden" id="repo-id" name="repo-id" value="{{ repo.id }}" />
    <button id="add-link-btn" class="button">Add URL</button>
</form>